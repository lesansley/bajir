<%= form_for([@surveytemplate, @surveytemplate.questions.build]) do |f| %>
  <p>
    <%= f.label :question %><br>
    <%= f.text_field :question %>
  </p>
  <p>
    <%= f.label :answerParameters %><br>
    <%= f.text_area :answerParameters %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>